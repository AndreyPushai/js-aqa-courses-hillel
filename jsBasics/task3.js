// Testing is adult
let age = 20;
let isAdult = age >= 18 ? true : false;
console.log(`Age: ${age}. Is adult: ${isAdult}`);

// Testing is not adult
age = 15;
isAdult = age >= 18 ? true : false;
console.log(`Age: ${age}. Is adult: ${isAdult}`);
